{
  "name": "hermes-model-leaderboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "LLM model leaderboard for Hermes Desktop (unified plugin: desktop/ + dashboard/).",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check desktop/plugin.js",
    "test": "bash tests/run_tests.sh",
    "test:ui": "node tests/ui/esm-render.mjs"
  },
  "dependencies": {
    "recharts": "^2.15.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.2"
  }
}
